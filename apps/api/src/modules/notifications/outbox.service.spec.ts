import { describe, it, expect } from '@jest/globals'

describe('OutboxService (unit) - skeleton', () => {
  it('should create and claim outbox events idempotently (TODO)', () => {
    // TODO: unit tests for createEventInTx, claimBatch, markDone/markFailed
    expect(true).toBe(true)
  })
})
